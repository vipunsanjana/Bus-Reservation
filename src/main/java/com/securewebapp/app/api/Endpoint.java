package com.securewebapp.app.api;

/**
 * Endpoint class defines the various endpoints used in the application.
 * It serves as a central location for managing endpoint paths.
 */
public class Endpoint {
    public static final String root = "/"; // Root endpoint
    public static final String login = "/login"; // Endpoint for login
    public static final String reservation = "/reservation"; // Endpoint for reservations
}
